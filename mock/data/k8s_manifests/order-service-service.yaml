apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shopfast-prod
spec:
  ports:
  - port: 8083
    protocol: TCP
    targetPort: 8083
  selector:
    app: order-service
  type: ClusterIP
