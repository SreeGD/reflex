apiVersion: v1
kind: Service
metadata:
  name: ehr-gateway
  namespace: medflow-prod
spec:
  ports:
  - port: 9080
    protocol: TCP
    targetPort: 9080
  selector:
    app: ehr-gateway
  type: ClusterIP
