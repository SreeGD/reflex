apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: shopfast-prod
spec:
  ports:
  - port: 8082
    protocol: TCP
    targetPort: 8082
  selector:
    app: cart-service
  type: ClusterIP
