apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: shopfast-prod
spec:
  ports:
  - port: 8085
    protocol: TCP
    targetPort: 8085
  selector:
    app: notification-service
  type: ClusterIP
