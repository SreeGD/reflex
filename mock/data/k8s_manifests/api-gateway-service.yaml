apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: shopfast-prod
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: api-gateway
  type: ClusterIP
