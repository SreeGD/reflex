apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: shopfast-prod
spec:
  ports:
  - port: 8086
    protocol: TCP
    targetPort: 8086
  selector:
    app: inventory-service
  type: ClusterIP
