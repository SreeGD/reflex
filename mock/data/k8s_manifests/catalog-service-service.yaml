apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: shopfast-prod
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: catalog-service
  type: ClusterIP
